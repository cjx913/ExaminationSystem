<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cjx913.es.mapper.UserMapper" >
   
    
    <resultMap id="BaseResultMap" type="userVO" >
        <id column="id" property="sysUser.id" jdbcType="VARCHAR" />
        <result column="account" property="sysUser.account" jdbcType="VARCHAR" />
        <result column="name" property="sysUser.name" jdbcType="VARCHAR" />
        <result column="password" property="sysUser.password" jdbcType="VARCHAR" />
        <result column="salt" property="sysUser.salt" jdbcType="VARCHAR" />
        <result column="locked" property="sysUser.locked" jdbcType="TINYINT" />
    </resultMap>

    <resultMap id="SysUserResultMap" type="sysUser" >
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="account" property="account" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="salt" property="salt" jdbcType="VARCHAR" />
        <result column="locked" property="locked" jdbcType="TINYINT" />
    </resultMap>

    <select id="selectAllSysUser" resultMap="SysUserResultMap">
        select id,account,name,password,salt,locked
        from sys_user
    </select>

    <select id="selectSysUserByUsername" parameterType="string" resultMap="SysUserResultMap">
        select id,account,name,password,salt,locked
        from sys_user
        where name=#{name}
    </select>
    
    <select id="selectSysUserByAccount" resultType="string" resultMap="SysUserResultMap">
        select id,account,name,password,salt,locked
        from sys_user,t_user_info
        where account=#{account} and sys_user.id=t_user_info.user_id
    </select>

    <insert id="insetSysUser" parameterType="sysUser">
    	<selectKey keyProperty="id" resultType="string" order="BEFORE">
    		select replace(uuid(), '-', '') as id from dual
    	</selectKey>
        insert into sys_user(id, account, name, password, salt, locked)
        values (#{id},#{account},#{name},#{password},#{salt},#{locked})
    </insert>
</mapper>